add_library(bsp INTERFACE)

target_include_directories(bsp INTERFACE include)

target_link_libraries(bsp INTERFACE common etl)

if (BUILD_EXECUTABLE STREQUAL "unitTest")

    add_subdirectory(mock)

endif ()
