# *******************************************************************************
# Copyright (c) 2024 Accenture
#
# This program and the accompanying materials are made available under the
# terms of the Apache License Version 2.0 which is available at
# https://www.apache.org/licenses/LICENSE-2.0
#
# SPDX-License-Identifier: Apache-2.0
# *******************************************************************************

name: Third party libraries check

on: [ merge_group, push, pull_request ]

jobs:
  rim:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Docker Environment
        uses: ./.github/actions/setup-docker-env

      - name: Run the third party libraries check via RIM inside the docker container
        run: DOCKER_UID=$(id -u) DOCKER_GID=$(id -g) DOCKER_HISTORY=/dev/null docker compose run --rm development rim status -w -d --verify-clean

  cargo-vendor:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Docker Environment
        uses: ./.github/actions/setup-docker-env

      - name: Verify libs/3rdparty/cargo-vendor matches `cargo vendor` output
        run: |
          DOCKER_UID=$(id -u) DOCKER_GID=$(id -g) DOCKER_HISTORY=/dev/null docker compose run --rm \
            -e CARGO_NET_OFFLINE=false \
            development bash -c '
              set -euo pipefail
              cargo vendor /tmp/cargo-vendor-check --locked --manifest-path Cargo.toml
              diff -rq libs/3rdparty/cargo-vendor /tmp/cargo-vendor-check
            '
